SELECT *
FROM app_user
ORDER BY username;

SELECT *
FROM simulation_config
ORDER BY id;

SELECT *
FROM simulation
ORDER BY id;

SELECT *
FROM simulation_file
ORDER BY id;

SELECT *
FROM simulation_log
ORDER BY id;

SELECT *
FROM simulation_metrics
ORDER BY simulation_id;